.container-fluid
  .album
    .row
      each team in teamsList
        .team.col-lg-3.col-md-6.text-center
          a(href=`/${team.tricode}-${team.teamId}`)
            img.rounded-circle(src=`https://www.nba.com/assets/logos/teams/primary/web/${team.tricode}.svg`, class='nba-team' width="150" height="150")
          h3= team.ttsName
          a#teamButton.btn.btn-light(href=`/${team.tricode}-${team.teamId}`)= 'View Players'
            |  »
